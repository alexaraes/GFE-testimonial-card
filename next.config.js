/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/GFE-testimonial-card',
    output: "export",  // <=== enables static exports
    reactStrictMode: true,
  };
  
  module.exports = nextConfig;